server:
  port: "8080"
  mode: "debug"  # debug, release, test

database:
  driver: "mysql"  # sqlite, mysql
  dsn: "root:As2024#xK9mPv7Rn@tcp(127.0.0.1:3306)/autostack?charset=utf8mb4&parseTime=True&loc=Local"

jwt:
  secret: "your-secret-key-change-in-production"
  expire_hour: 24

crypto:
  secret_key: "kGi9gkEYeaK5uSyI73YOCcy4Py+eaOty"  # 必须32字节，用于加密API凭证

